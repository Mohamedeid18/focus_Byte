# FocusByte

A minimalist Task & Pomodoro timer built with Flutter.

## Run

1. Install Flutter SDK: https://flutter.dev
2. From the `focus_byte` folder run:

```bash
flutter pub get
flutter run
```

The app uses `shared_preferences` to persist tasks and timer state.
